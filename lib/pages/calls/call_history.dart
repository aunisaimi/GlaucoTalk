import 'package:flutter/material.dart';


class CallHistory extends StatelessWidget {
  const CallHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("Time to make history..",
        style: TextStyle(fontSize: 20),),
      ),
    );
  }
}
